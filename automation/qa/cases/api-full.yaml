# GENERATED by scripts/generate_api_cases.py - regenerate, don't hand-edit the swept section.
- id: get-api-alerts
  type: api
  method: GET
  path: /api/alerts
  expect_status: [200]
  description: GET sweep of /api/alerts
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-analysis-plantid
  type: api
  method: GET
  path: /api/analysis/smoke-park-one?domain=soiling
  expect_status: [200, 404]
  description: GET sweep of /api/analysis/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-analytics-plant-config-plantid
  type: api
  method: GET
  path: /api/analytics/plant-config/smoke-park-one
  expect_status: [200, 404]
  description: GET sweep of /api/analytics/plant-config/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-faults
  type: api
  method: GET
  path: /api/bess/faults
  expect_status: [200, 404]
  description: GET sweep of /api/bess/faults
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-fleet-health
  type: api
  method: GET
  path: /api/bess/fleet/health
  expect_status: [200, 404]
  description: GET sweep of /api/bess/fleet/health
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-assets-assetid-cycling
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/assets/primary/cycling
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/assets/[assetId]/cycling
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-assets-assetid-dispatch
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/assets/primary/dispatch
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/assets/[assetId]/dispatch
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-assets-assetid
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/assets/primary
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/assets/[assetId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-assets-assetid-warranty
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/assets/primary/warranty
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/assets/[assetId]/warranty
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-assets-assetid-warranty-violations
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/assets/primary/warranty/violations
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/assets/[assetId]/warranty/violations
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-audit-pdf
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/audit/pdf
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/audit/pdf
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-audit
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/audit
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/audit
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-curtailment
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/curtailment
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/curtailment
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-hybrid
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/hybrid
  expect_status: [200, 404]
  description: GET sweep of /api/bess/plants/[plantId]/hybrid
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid-revenue
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage/revenue
  expect_status: [200]
  description: GET sweep of /api/bess/plants/[plantId]/revenue
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-bess-plants-plantid
  type: api
  method: GET
  path: /api/bess/plants/region_a-storage
  expect_status: [200]
  description: GET sweep of /api/bess/plants/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-billing-plan
  type: api
  method: GET
  path: /api/billing/plan
  expect_status: [200]
  description: GET sweep of /api/billing/plan
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-billing-usage
  type: api
  method: GET
  path: /api/billing/usage
  expect_status: [200]
  description: GET sweep of /api/billing/usage
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-conversations-id
  type: api
  method: GET
  path: /api/chat/conversations/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/chat/conversations/[id]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-conversations
  type: api
  method: GET
  path: /api/chat/conversations
  expect_status: [200]
  description: GET sweep of /api/chat/conversations
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-kb-id
  type: api
  method: GET
  path: /api/chat/kb/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/chat/kb/[id]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-kb
  type: api
  method: GET
  path: /api/chat/kb
  expect_status: [200]
  description: GET sweep of /api/chat/kb
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-scope-plants-slug-inverters
  type: api
  method: GET
  path: /api/chat/scope/plants/smoke-park-one/inverters
  expect_status: [200]
  description: GET sweep of /api/chat/scope/plants/[slug]/inverters
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-chat-scope-plants
  type: api
  method: GET
  path: /api/chat/scope/plants
  expect_status: [200]
  description: GET sweep of /api/chat/scope/plants
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-checkout-residential
  type: api
  method: GET
  path: /api/checkout/residential
  expect_status: [200]
  description: GET sweep of /api/checkout/residential
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-connections-connectionid-discover
  type: api
  method: GET
  path: /api/connections/00000000-0000-0000-0000-000000000000/discover
  expect_status: [404, 400, 410]
  description: GET sweep of /api/connections/[connectionId]/discover
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-connections-connectionid-generate-config
  type: api
  method: GET
  path: /api/connections/00000000-0000-0000-0000-000000000000/generate-config
  expect_status: [404, 400, 410]
  description: GET sweep of /api/connections/[connectionId]/generate-config
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-connections-connectionid-mappings
  type: api
  method: GET
  path: /api/connections/00000000-0000-0000-0000-000000000000/mappings
  expect_status: [404, 400, 410]
  description: GET sweep of /api/connections/[connectionId]/mappings
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-connections-connectionid
  type: api
  method: GET
  path: /api/connections/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/connections/[connectionId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-connections
  type: api
  method: GET
  path: /api/connections
  expect_status: [200]
  description: GET sweep of /api/connections
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-cron-evaluate-alerts
  type: api
  method: GET
  path: /api/cron/evaluate-alerts
  expect_status: [200]
  description: GET sweep of /api/cron/evaluate-alerts
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-cron-ingest-docs
  type: api
  method: GET
  path: /api/cron/ingest-docs
  expect_status: [200]
  description: GET sweep of /api/cron/ingest-docs
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-dashboards-id
  type: api
  method: GET
  path: /api/dashboards/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/dashboards/[id]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-dashboards-public-token
  type: api
  method: GET
  path: /api/dashboards/public/unknown-token
  expect_status: [404, 400, 410]
  description: GET sweep of /api/dashboards/public/[token]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-dashboards
  type: api
  method: GET
  path: /api/dashboards
  expect_status: [200]
  description: GET sweep of /api/dashboards
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-digitaltwin-plantid-inverter-metrics
  type: api
  method: GET
  path: /api/digitaltwin/smoke-park-one/inverter-metrics
  expect_status: [200]
  description: GET sweep of /api/digitaltwin/[plantId]/inverter-metrics
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-digitaltwin-plantid-inverter-rank-history
  type: api
  method: GET
  path: /api/digitaltwin/smoke-park-one/inverter-rank-history
  expect_status: [200]
  description: GET sweep of /api/digitaltwin/[plantId]/inverter-rank-history
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-digitaltwin-plantid-parquet-query
  type: api
  method: GET
  path: /api/digitaltwin/smoke-park-one/parquet-query?device_id=INV-01&type=mppt_voltage
  expect_status: [200, 400, 404]
  description: GET sweep of /api/digitaltwin/[plantId]/parquet-query
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-digitaltwin-plantid-summary
  type: api
  method: GET
  path: /api/digitaltwin/smoke-park-one/summary
  expect_status: [200]
  description: GET sweep of /api/digitaltwin/[plantId]/summary
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-digitaltwin-plantid-timeseries
  type: api
  method: GET
  path: /api/digitaltwin/smoke-park-one/timeseries?device_id=INV-01
  expect_status: [200, 404]
  description: GET sweep of /api/digitaltwin/[plantId]/timeseries
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-faults
  type: api
  method: GET
  path: /api/faults?plantId=smoke-park-one
  expect_status: [200, 404]
  description: GET sweep of /api/faults
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-faults-sensor-history
  type: api
  method: GET
  path: /api/faults/sensor-history?plantId=smoke-park-one&sensorId=1
  expect_status: [200, 400, 404]
  description: GET sweep of /api/faults/sensor-history
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-hydrogen-plants-plantid
  type: api
  method: GET
  path: /api/hydrogen/plants/smoke-park-one
  expect_status: [200]
  description: GET sweep of /api/hydrogen/plants/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-integrations-webhooks
  type: api
  method: GET
  path: /api/integrations/webhooks
  expect_status: [200]
  description: GET sweep of /api/integrations/webhooks
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-inverters-inverterid-anomalies
  type: api
  method: GET
  path: /api/inverters/INV-01/anomalies?plant_id=smoke-park-one
  expect_status: [200, 400, 404]
  description: GET sweep of /api/inverters/[inverterId]/anomalies
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-inverters-inverterid-classification
  type: api
  method: GET
  path: /api/inverters/INV-01/classification?plant_id=smoke-park-one
  expect_status: [200, 400, 404]
  description: GET sweep of /api/inverters/[inverterId]/classification
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-llm-fleet-insights
  type: api
  method: GET
  path: /api/llm/fleet-insights
  expect_status: [200, 404]
  description: GET sweep of /api/llm/fleet-insights
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-llm-usage
  type: api
  method: GET
  path: /api/llm/usage
  expect_status: [200, 404]
  description: GET sweep of /api/llm/usage
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-mcp-keys-audit
  type: api
  method: GET
  path: /api/mcp-keys/audit
  expect_status: [200]
  description: GET sweep of /api/mcp-keys/audit
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-mcp-keys
  type: api
  method: GET
  path: /api/mcp-keys
  expect_status: [200]
  description: GET sweep of /api/mcp-keys
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-measurements-plantid
  type: api
  method: GET
  path: /api/measurements/smoke-park-one
  expect_status: [200, 404]
  description: GET sweep of /api/measurements/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-alerts
  type: api
  method: GET
  path: /api/plants/smoke-park-one/alerts
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/alerts
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-classifications
  type: api
  method: GET
  path: /api/plants/smoke-park-one/classifications
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/classifications
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-contracts
  type: api
  method: GET
  path: /api/plants/smoke-park-one/contracts
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/contracts
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-data-lineage
  type: api
  method: GET
  path: /api/plants/smoke-park-one/data-lineage
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/data-lineage
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-data-sources
  type: api
  method: GET
  path: /api/plants/smoke-park-one/data-sources
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/data-sources
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-inverter-groups
  type: api
  method: GET
  path: /api/plants/smoke-park-one/inverter-groups
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/inverter-groups
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-live
  type: api
  method: GET
  path: /api/plants/smoke-park-one/live
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/live
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-onboarding-status
  type: api
  method: GET
  path: /api/plants/smoke-park-one/onboarding-status
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/onboarding-status
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-power-forecast
  type: api
  method: GET
  path: /api/plants/smoke-park-one/power-forecast
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/power-forecast
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid
  type: api
  method: GET
  path: /api/plants/smoke-park-one
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-settings
  type: api
  method: GET
  path: /api/plants/smoke-park-one/settings
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/settings
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants-plantid-source-candidates
  type: api
  method: GET
  path: /api/plants/smoke-park-one/source-candidates
  expect_status: [200]
  description: GET sweep of /api/plants/[plantId]/source-candidates
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-plants
  type: api
  method: GET
  path: /api/plants
  expect_status: [200]
  description: GET sweep of /api/plants
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-quality-plants-plantid-curation-log
  type: api
  method: GET
  path: /api/quality/plants/smoke-park-one/curation-log
  expect_status: [200]
  description: GET sweep of /api/quality/plants/[plantId]/curation-log
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-quality-plants-plantid-incidents
  type: api
  method: GET
  path: /api/quality/plants/smoke-park-one/incidents
  expect_status: [200]
  description: GET sweep of /api/quality/plants/[plantId]/incidents
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-reports-id
  type: api
  method: GET
  path: /api/reports/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/reports/[id]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-reports
  type: api
  method: GET
  path: /api/reports
  expect_status: [200]
  description: GET sweep of /api/reports
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-inverters-inverterid
  type: api
  method: GET
  path: /api/soiling/inverters/INV-01
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/inverters/[inverterId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-aod
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/aod
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/aod
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-cleaning-schedule
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/cleaning-schedule
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/cleaning-schedule
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-cleaning-schedules
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/cleaning-schedules
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/cleaning-schedules
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-digital-twin
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/digital-twin
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/digital-twin
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-forecast
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/forecast
  expect_status: [200]
  description: GET sweep of /api/soiling/plants/[plantId]/forecast
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-history
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/history?inverterId=INV-01&days=30
  expect_status: [200, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/history
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-inverters-sr-estimate
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/inverters/sr-estimate
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/inverters/sr-estimate
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-labels
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/labels
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/labels
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-quality-correlation
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/quality/correlation
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/quality/correlation
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-quality-deep-signals
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/quality/deep-signals
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/quality/deep-signals
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-quality-irradiance
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/quality/irradiance
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/quality/irradiance
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-quality-spatial
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/quality/spatial
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/quality/spatial
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-quality-today
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/quality/today
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/quality/today
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-rain
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/rain
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/rain
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-rain-forecast
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/rain-forecast
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/rain-forecast
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-streams
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/streams
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/streams
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-summary
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/summary
  expect_status: [200]
  description: GET sweep of /api/soiling/plants/[plantId]/summary
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-plants-plantid-zones
  type: api
  method: GET
  path: /api/soiling/plants/smoke-park-one/zones
  expect_status: [200, 400, 404]
  description: GET sweep of /api/soiling/plants/[plantId]/zones
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-soiling-universal
  type: api
  method: GET
  path: /api/soiling/universal
  expect_status: [200, 400]
  description: GET sweep of /api/soiling/universal
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-team-members
  type: api
  method: GET
  path: /api/team/members
  expect_status: [200]
  description: GET sweep of /api/team/members
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-tickets-ticketid-comments
  type: api
  method: GET
  path: /api/tickets/00000000-0000-0000-0000-000000000000/comments
  expect_status: [404, 400, 410]
  description: GET sweep of /api/tickets/[ticketId]/comments
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-tickets-ticketid
  type: api
  method: GET
  path: /api/tickets/00000000-0000-0000-0000-000000000000
  expect_status: [404, 400, 410]
  description: GET sweep of /api/tickets/[ticketId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-tickets-export
  type: api
  method: GET
  path: /api/tickets/export
  expect_status: [200]
  description: GET sweep of /api/tickets/export
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-tickets
  type: api
  method: GET
  path: /api/tickets
  expect_status: [200]
  description: GET sweep of /api/tickets
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-tickets-stats
  type: api
  method: GET
  path: /api/tickets/stats
  expect_status: [200]
  description: GET sweep of /api/tickets/stats
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-faults
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/faults
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/faults
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-labeled-events
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/labeled-events
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/labeled-events
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-power-curve
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/power-curve
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/power-curve
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-rul
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/rul
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/rul
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-scada
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/scada?turbineId=T-01
  expect_status: [200, 400, 404]
  description: GET sweep of /api/wind/plants/[plantId]/scada
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-site
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/site
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/site
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: get-api-wind-plants-plantid-turbines
  type: api
  method: GET
  path: /api/wind/plants/smoke-park-one/turbines
  expect_status: [200, 404]
  description: GET sweep of /api/wind/plants/[plantId]/turbines
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: post-api-tickets-create
  type: api
  method: POST
  path: /api/tickets
  json: {"plant_id": "__PLANT_UUID__", "title": "QA suite ticket (safe to close)", "trigger_type": "MANUAL_CREATION", "priority": "LOW"}
  expect_status: [201, 404]
  description: Create a low-priority ticket on the seeded plant (404 acceptable when plant uuid placeholder not replaced).
  rubric: |
    Response is valid JSON with no stack traces or secrets. Status matches expect_status. For 200s the payload has plausible domain fields (not an empty shell when the seeded plant should have data). For 4xx the body carries an 'error' key with a human-readable reason.
- id: post-api-plants-mw-cap
  type: api
  method: POST
  path: /api/plants
  json: {"name": "QA Overcap Park", "latitude": 37.0, "longitude": -4.0, "capacity_mw": 9999}
  expect_status: [402, 401]
  description: MW cap must reject an absurd plant (402 upgrade-required; 401 when unauthenticated on staging).
  rubric: |
    Body has error=mw_limit_reached (or plant_limit_reached) and an upgrade_url when 402.
- id: post-api-plants-sample-feed
  type: api
  method: POST
  path: /api/plants/smoke-park-one/sample-feed
  json: {}
  expect_status: [200]
  description: Sample inverter feed provisions a sandbox connection + synthetic telemetry.
  rubric: |
    Body has ok=true, a connection_id, device_count>0 and rows_written>0. Idempotent.
