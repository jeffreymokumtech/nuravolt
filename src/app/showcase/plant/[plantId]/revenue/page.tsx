'use client';

/**
 * Showcase revenue, delegates to the /demo route body (showcase layout
 * provides the showcase dataRoot).
 */
import DemoPage from '@/app/demo/plant/[plantId]/revenue/page';

export default function ShowcaseRevenuePage() {
  return <DemoPage />;
}
