'use client';

/**
 * Showcase optimizer audit, delegates to the /demo route body (the showcase
 * layout provides the showcase dataRoot + PlantPageChrome).
 */
import DemoPage from '@/app/demo/plant/[plantId]/audit/optimizer/page';

export default function ShowcaseOptimizerAuditPage() {
  return <DemoPage />;
}
