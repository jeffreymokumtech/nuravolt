'use client';

/**
 * Showcase warranty evidence, delegates to the /demo route body (the showcase
 * layout provides the showcase dataRoot + PlantPageChrome).
 */
import DemoPage from '@/app/demo/plant/[plantId]/audit/warranty/page';

export default function ShowcaseWarrantyEvidencePage() {
  return <DemoPage />;
}
